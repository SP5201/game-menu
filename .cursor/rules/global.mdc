---
description: QDesktop 全局约定；工程/Git/编译；UI 与功能分层见 ui/feature.mdc
alwaysApply: true
---

# QDesktop 全局约定

## 1. 基础与 Agent

- **技术栈**：Delphi XE2（Win32 + Win64）+ XCGUI；依赖优先 GitHub 开源（须兼容 XE2）；**禁止** `Vcl.*`。**回复中文**。
- **复杂任务先方案**：架构变动、跨模块重构、性能热路径大改、新子系统引入等，须先 Markdown 方案，**待用户确认后再动代码**。

## 2. 工程与源码

- **联动**：`*.pas` 增删改名须同步 `QDesktop.dproj`、`QDesktop.dpr`。
- **编码 / bat / cmd**：见 **`encoding.mdc`**。
- **分层**（详细规则按目录自动加载）：
  - **UI**：`窗口/`、`样式/`、`Resource/Layout/` → **`ui.mdc`**
  - **功能**：`功能/` → **`feature.mdc`**
  - **禁止改** `库/`
- **GitHub 同步含删除**：须 `git add -A`；本地已删的已跟踪文件必须一并提交；推送前 `git status` 核对。
- **本地数据库不上传**：`Debug/**/Data/games.db`、`Debug/**/Data/Everything.db`（及 `Debug/**/Bin/Everything.db`）**禁止**提交；远程若曾存在须 `git rm --cached`。

## 3. 通用编码原则

- **最小改动**；**禁止过度封装**（1～2 行不抽函数、不为极 unlikely 边界堆 `try/except`）。
- **先读再写**：匹配周边命名与分层；优先扩展已有模块。
- **替换即删除**：新封装取代旧调用时同步删旧实现；**仅当用户明确要求**才做向后兼容。
- **注释节制**：仅非显而易见的业务规则、XCGUI/D2D/WOW64 陷阱。

## 4. 改码后编译（必做）

- 改 `*.pas` / `*.dpr` / `*.dproj` 后须**编译通过**再汇报；仅 Layout/资源/文档/规则且未动 Pascal 可跳过。

```bat
cd /d "d:\游戏菜单"
编译测试.bat nopause
```

- 须经 **cmd** 运行（见 `encoding.mdc`）；改 `编译测试.bat` 后须跑通再汇报。
